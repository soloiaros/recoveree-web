# Recoveree Web

Coach's web dashboard for the Recoveree sports recovery platform.

```bash
npm install
cp .env.example .env   # fill in Supabase URL + anon key
npm run dev
```

Full architecture / handoff notes live in [`CODEBASE.md`](./CODEBASE.md).
